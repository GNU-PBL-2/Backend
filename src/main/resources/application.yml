spring :
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  application :
    name: pbl2
  jpa:
    open-in-view: false
    defer-datasource-initialization: true



jwt:
  secret-key: ${JWT_SECRET_KEY}
  accessToken-expiration-millis: 10000000000 # ?? ??